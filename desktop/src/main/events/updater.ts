import { BrowserWindow } from 'electron'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function registerUpdater(_win: BrowserWindow): void {
  // Auto-update disabled for X86 fork — distributes via direct download
}
